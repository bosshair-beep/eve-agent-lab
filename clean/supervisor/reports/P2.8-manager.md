# P2.8 WS handshake forensic — Manager

Independent Auditor + Verifier. No new WebSocket. Implementer disabled.

## Root cause

**DEVICE_AUTH/BINDING** (STRONG_INFERENCE, both agents).

Live `server.auth.enabled=true` (Redis/manager-api, not yaml `false`). Token HMAC is `client_id|device_id|ts`. P2.7 reused physical Device-Id `30:ed:a0:2a:1b:d8` and scraped Bearer, then minted a **new** `uuid4` Client-Id. Verify fails → `_handle_auth` sends `认证失败` → `websocket.close()` default **1000** with **no INFO**. `ConnectionHandler` never ran, so no `conn - Headers`.

Observed: same-second `received 1000 (OK); then sent 1000 (OK)`; empty docker log window; `ws: {}`.

## Protocol fact

`listen/state=detect` + `text` **is** a supported user-turn path (`listenMessageHandler.py`). P2.7 intended that **after** hello-reply. Hello-reply never arrived, so **detect was never sent**. Direct-text assumption is true; P2.7 did not exercise it.

## P2.9 (not executed)

Client: protocol-compatible synthetic WS **or** physical device. If synthetic: reuse the **exact** 19:19 Client-Id + Device-Id + Bearer triad; persist non-JSON recv; MAX_WS_SESSIONS=1; MAX_USER_TURNS=1; MAX_RETRIES=0. Requires Supervisor approval (auth reuse + LLM/TTS + title POST on close).

SAFE_TO_RUN_P29=NO
