# GitHub capability (P2.6 discovery)

Do not store credentials in this file.

## VPS (`eve-vps`, Clean host)

- git: 2.43.0 present
- gh CLI: **not installed**
- `git config --global` identity: unset
- GitHub SSH (`git@github.com`): Permission denied (publickey)
- `/opt/xiaozhi-clean` is **not** a git repository
- `/opt/xiaozhi-clean/source` remote: `https://github.com/xinnan-tech/xiaozhi-esp32-server.git` (upstream, unrelated to this bridge)
- Credential helper: none

VPS classification: **GITHUB_AUTH_REQUIRED** for push/create from the Clean host.

## Manager workstation (Windows Cursor host)

- git: 2.55.0.windows.3
- gh CLI: 2.98.0, logged in to github.com account `bosshair-beep`
- protocol: HTTPS
- token scopes observed via `gh auth status`: gist, read:org, repo, workflow
- SSH to github.com from this host: host key verification failed (HTTPS gh is the working path)

Workstation classification: **GITHUB_READY** for repository create/push using existing `gh` login. No new credentials created.

## Human action if VPS must push itself

1. Do **not** copy tokens onto the VPS unless a human explicitly approves.
2. Either install `gh` on the VPS (requires human approval for package install) and `gh auth login`, **or** add a GitHub deploy key / SSH key with write access to `eve-agent-lab`.
3. Then set bridge remote: `git -C /opt/xiaozhi-clean/agent-lab/.github-bridge remote add origin git@github.com:<owner>/eve-agent-lab.git`

Until then, publish from the authenticated workstation; VPS keeps the local control plane.
