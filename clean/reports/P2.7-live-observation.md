# P2.7 controlled live observation — STOP

TEST_CASE=get_lunar  
Authorization: exactly one bound WS session, one user turn, zero retries.

## Pre-flight

PASS. `xz-clean-server` running, RestartCount=0 (unchanged since 2026-09-19T12:16:29Z), prompt SHA-256 `3407a389979f5da0db2b1f929e253709cf5dee1165e1d90d6351a904d91aab83` host+container, compose SHA-256 `e6073c48f93e9b7eba4fb5a8cc1a47dcb8a3d2a289aa8bffdaf92648441d50dc`, CURRENT.json `automation_enabled=false` `global_stop=false` `implementer_enabled=false`.

## Live attempt (ONE)

Client ran **inside** `xz-clean-server` (host Python has no `websockets`; no package install) to `ws://127.0.0.1:8000/xiaozhi/v1/` with Device-Id `30:ed:a0:2a:1b:d8` (same MAC as prior bound 19:19 session). Intended user text was not confirmed delivered.

**Result:** `ConnectionClosedOK: received 1000 (OK)` within the same UTC second (`t0=t1=2026-09-19T17:08:07Z`). `session_id` not captured. TTS stop not seen.

**Logs:** `docker logs --since 3m` and `--since 10m` for that window were **empty**. `--tail 40` still ends at **19:24:23 +0700** (previous physical-device session, not this test). No `收到hello消息` / `收到listen消息` / `大模型收到用户消息` / `执行工具` for the authorized utterance.

Therefore this phase **did not** produce a usable conversation/tool pipeline trace.

Per budget: MAX_TEST_RETRIES=0. **No reconnect. No alternate wording. No second agent live test.**

## Answers (this turn)

1. Did Vietnamese input invoke get_lunar? **UNKNOWN** (user turn not observed on server)
2. Tool args? **NOT_OBSERVABLE**
3. CJK layer? **UNKNOWN**
4. Unexpected tool? **UNKNOWN** (no tool INFO)
5. Spoken text language? **NOT_OBSERVABLE**
6. Static vs live match? **NOT_COMPARABLE** (no live turn)

## Side effects (this attempt)

CHAT_TITLE_POST=UNKNOWN (no new disconnect line after 19:24:23)  
DB_TITLE_WRITE=UNKNOWN (no DB query performed)  
QWEN_USAGE=UNKNOWN (no Token/消耗 line in this window)  
EDGETTS_USAGE=UNKNOWN  
QWEATHER_PREFETCH=UNKNOWN  
IP_LOOKUP=UNKNOWN  

Connect-time prefetch / title POST **cannot** be confirmed for this socket because the server INFO stream does not show the connection.

## Safety

Runtime/source/prompt/compose/DB not modified. Implementer remains disabled. automation_enabled remains false.

## Recommended next ticket (Supervisor)

T-P2.8: Diagnose in-container WS close 1000 with **no new docker logs**, using **read-only** inspection only until a **new** live-session authorization is granted. Do not treat P2.5 historical 19:19–19:24 logs as this test’s live turn.
